{
	"name": "lumina-van",
	"version": "1.0.0",
	"private": true,
	"description": "Lumina — editorial premium van & travel landing page on Cloudflare Workers",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.34.0"
	}
}
