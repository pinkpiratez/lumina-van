{
	"name": "lumina-van",
	"version": "1.0.0",
	"private": true,
	"description": "Lumina Fleet — isometric 3D factory commuter van transport on Cloudflare Workers",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.34.0"
	}
}
